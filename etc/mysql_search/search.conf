# MySQL Monitoring Daemon Main Configuration File
# Defines database connection information and monitoring policies.

# First database connection settings
db_host=localhost
db_user=monitor_user
db_id=0
monitor_interval_sec=3600

# Second database connection settings
db_host=db-dev-02.example.com
db_user=dev_monitor
db_id=1
monitor_interval_sec=600

# How to Write Prometheus Metrics Files
# 1 = Truncate the file and write the latest state (default)
# 0 = Append to the file (deprecated)
prometheus_truncate=1
